## What this changes



## Why



## Checks

- [ ] `bun run check` passes
- [ ] `cargo test -p pob-redux --lib` and `cargo test -p pob-engine` pass, if Rust or `bridge.lua` changed
- [ ] Engine changes work in both games (`--pob-root src-tauri/resources/pob` and `src-tauri/resources/pob1`)
- [ ] I opened a real build and compared the sidebar with Path of Building
- [ ] Screenshots are attached, if the UI changed
